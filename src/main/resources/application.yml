spring:
  application:
    name: novabank-api
  datasource:
    url: jdbc:postgresql://localhost:5432/novabank
    username: postgres
    password: 745507
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  secret: "ClaveSecretaDeAlMenos32CaracteresParaSeguridad"
  expiration: 86400000

springdoc:
  swagger-ui:
    path: /swagger-ui.html